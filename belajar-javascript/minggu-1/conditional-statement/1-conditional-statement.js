// Conditional statements (digunakan untuk melakukan aksi berdasarkan kondisi)
/* 

// if statement (blok kode akan dieksekusi jika kondisi bernilai true)

if (kondisi == true) {
    // jalankan kode ini
}

// else statement (jika kondisi bernilai false, maka eksekusi blok kode yang ada di else statement)

if (kondisi == true) {
    // jalankan kode ini
} else {
    // jika kondisi = false jalankan kode ini
}

// else if statement (untuk menambah kondisi baru jika kondisi 1 bernilai false)

if (kondisi1 == true) {
    // jalankan kode ini
} else if (kondisi 2 == true) {
    // jika kondisi 1 = false dan kondisi 2 = true jalankan kode ini
} else {
    // jika kondisi 1 dan kondisi 2 = false jalankan kode ini
}

// netsed if (if bersarang)

if (kondisi == true){
    // jalankan kode ini
} else {
    if (kondisi == true) {
        // jalankan kode ini
    } else if (kondisi == true) {
        // jalankan kode ini
    } else {
        // jalankan kode ini
    }
}
*/
